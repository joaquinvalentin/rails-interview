<%= link_to 'Add Todo List', new_todo_list_path, class: "btn btn-primary" %>

<div class="mt-5">
  <% @todo_lists.each do |todo_list| %>
    <%= link_to todo_list_path(todo_list), class: "flex items-center w-full gap-2 p-2 border-b border-gray-200 " do %>
      <div class="p-2 text-center">
        #<%= todo_list.id %> | <%= todo_list.name %>
      </div>
      <div class="mt-5"></div>
    <% end %>
  <% end %>
</div>